#version 450

layout (local_size_x = 32, local_size_y = 1, local_size_z = 1) in;

layout (set = 0, binding = 0) buffer input_data
{
  float data[];
} uinput;

void main() 
{
  uint idx = gl_GlobalInvocationID.x;
  // uinput.data[idx] = uinput.data[idx] * uinput.data[idx];
  uinput.data[idx] = float(idx);
}
